import React from "react";
import styles from "./TasksContainer.module.css";

const TasksContainer: React.FC = () => {
  return <div className={styles.taskcontainer}>TaskContainer</div>;
};

export default TasksContainer;
